# Add your product logic here

Create a folder per feature, e.g. `src/features/widgets/`, with:

- `actions.ts` — Server Actions
- `components/` — feature UI

Register routes under `src/app/(dashboard)/your-feature/page.tsx`.

Auth, billing, email, and org scaffolding are already wired. Focus on your domain.
